## 改了什么
（一句话；可列点）

## 依据
- [ ] 术语已查 `汉化相关\` 对照表（涉及中英互译时必选）
- [ ] 数值来源标注（定义文件 / 游戏内 / wiki 链接 / 实测条件）
- [ ] 未引入本机绝对路径（一律用 `<WS>` `<HOME>` 等占位符）
- [ ] 未改动 `.gitignore` 排除集内容（`_tools\`、`pony IDE\`、`.workbuddy\` 等不入库）

## 影响范围
（改动的文件 / 章节 / 是否影响既有数据结论）

## 自测
（跑过的脚本 / 抽查的条目，例如 `sw_defs.py get ...`、`sw_lookup.py ...`）

---
关联 Issue：#（如有）
