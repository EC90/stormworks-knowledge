# 共建指南（CONTRIBUTING.md）

> 仓库：`github.com/EC90/stormworks-knowledge`（公开）· 许可：CC BY-NC-SA 4.0
> 本工作区是 **Stormworks: Build and Rescue** 的中文资料整理库——
> 术语基准、部件数据、机制原理、Lua 手册、实测归档与工具链。

欢迎任何人共建。**贡献形式不限**：纠错、补数据、新实测、脚本改进、文档润色皆可。

---

## 一、共建流程（标准 GitHub 流程）

1. **先开 Issue 讨论**（尤其改动面较大时）：勘误、新增方向、机制疑问。
2. **Fork 本仓库** → 在 fork 上开分支改动 → 提交 → **Pull Request**。
3. 维护者审阅合入。PR 描述请写清：**改了什么、为什么、依据来源**。
4. 简单勘误（错别字、链接、明显笔误）也可直接 PR，无需先开 Issue。

仓库当前由 `EC90` 维护。合入规则宽松，但**三条红线与数据纪律不妥协**（见下）。

---

## 二、硬性纪律（合入前必读）

### 1. 术语必须查表，禁止意译
涉及游戏内容（部件名、UI、Lua 节点、任务/地名/生物）的中英互译，
**一律以 `汉化相关\` 对照表为准**，不要凭语感翻译。

```bash
python "汉化相关/脚本/sw_check.py" --auto          # 新鲜度检查（过期自动重建）
python "汉化相关/脚本/sw_lookup.py" "modular engine cylinder"
```

### 2. 不提交本机专属内容
仓库自带了 `.gitignore`，**不要 force-add 或绕开它**。以下永不入库：

- `_tools\`（steamcmd、gh CLI 等本地工具；协作者自行下载）
- `pony IDE\`（第三方 IDE 源码副本）
- `.workbuddy\` / `.workbuddy-ai\`（AI 应用的记忆日志、自动化、目录联结）
- `工作区导航\路径配置.json`（**本机路径实测值**，`sw_paths.py --set/--write` 会按机器重建）
- `_work\` 等临时目录、`*.dmp` 崩溃转储

### 3. 路径一律用占位符，禁止写死本机地址
`C:\Users\<用户名>`、`E:\SteamLibrary` 等**因机器而异**，文档/脚本只写
`<WS>` `<HOME>` `<SW_GAME>` `<SW_WORKSHOP>` `<PY>` 等占位符。

```bash
python "工作区导航/脚本/sw_paths.py"     # 解析占位符 → 本机真实路径；--json / --write
```

约定全文：`工作区导航\07_路径占位符约定.md`。
⚠ 你的个人实测记录请写在文档占位符说明里，**不要把带用户名的绝对路径提交上来**。

### 4. 数据来源要标注
- **游戏数据提取物**（部件规格、选项枚举、mesh 几何等）：来源为游戏定义文件，请注明
  「🎮 来源：游戏定义文件 <文件名>」。
- **汉化对照数据**：源自创意工坊汉化补丁（原作者保留版权），仅供学习整理，勿商用。
- **工坊 Lua 语料**（`AI相关\` 下提取件）：版权归各工坊作者；每份提取件带 `-- source:`/`-- url:` 头部，**勿删除**，引用时保留署名。
- **第三方工具二进制**（如 BKN 工具、SW示波器）：版权归原作者。

### 5. 数值以游戏内为真
精确数值先查 `数据库\方块数据\脚本\sw_defs.py`（定义文件为唯一真值），
查不到才实测并注明条件；数据权威性：定义文件 > 游戏内文本 > 日文 wiki > Fandom/社区。

---

## 三、本地环境准备（克隆后 5 分钟上手）

```bash
# 1) 解析占位符到本机（按提示定位缺项；MISS 排查见 sw_paths.py 输出）
python "工作区导航/脚本/sw_paths.py"
python "工作区导航/脚本/sw_paths.py" --write      # 回写 路径配置.json（已被 gitignore）

# 2) 术语表新鲜度（首次会重建，之后毫秒级）
python "汉化相关/脚本/sw_check.py" --auto

# 3) 新会话/新人读这三个文件（按序）
README.md  →  AGENTS.md  →  工作区导航/01_接手清单.md
```

无需 Steam 即可浏览全部知识册与脚本；只有跑「游戏实测类」脚本才需要游戏本体/存档
（且对游戏与工坊目录**只读**——见 AGENTS.md §1）。

---

## 四、提交规范

- 提交信息用**简体中文**、一句话说清改动，如：
  `修正 17_传感器与雷达.md §2.6 欧拉角单位表述` / `新增 方块数据：xxx 部件几何`
- 不做 `force push` 到 `main`；大改动请拆分提交，便于审阅。
- **新增超过 50MB 的二进制前先在 Issue 讨论**（GitHub 单文件硬限 100MB）。
- 本地工作日志（`.workbuddy\memory\`）是个人笔记，**无需也不应提交**。

---

## 五、目录速查

| 目录 | 内容 |
| --- | --- |
| `工作区导航\` | 接手清单 / 目录地图 / 脚本清单 / 环境依赖 / 长期知识 |
| `技能库\` | 7 个 Stormworks 技能（XML 编辑、实测归档、mesh、wiki 转册等） |
| `汉化相关\` | 中英对照表（术语基准）+ 生成/查询脚本 |
| `数据库\` | 知识册（部件/机制/Lua/PID）+ BKN 工具 + 本地 wiki + 示波器数据 |
| `AI相关\` | 工坊 Lua 语料提取流水线（保留溯源） |

完整地图：`工作区导航\02_目录地图.md`。

感谢共建。
