# Stormworks 术语对照指南（AI 必读）

> **一句话规则**：涉及 Stormworks 游戏内容的中英互译，必须先查 `<WS>\汉化相关\` 的对照表，
> 采用表中既有译法，**禁止凭语感意译**。

**术语基准目录：`<WS>\汉化相关\`（`D:\STORMWORKS\汉化相关\`）**（入口：`README_中英对照表.md`）

> 📌 **路径占位符**：`<WS>`（工作区根）、`<PY>`（Python 解释器）、`<SW_LANG_TSV>`（汉化补丁 language.tsv）
> 都是**因机器而异**的地址——用户名、盘符、Steam 库位置换台机器就变。
> 解析：`python "<WS>/工作区导航/脚本/sw_paths.py"`；约定与排查见 `工作区导航\07_路径占位符约定.md`。

---

## 1. 触发条件（命中任一即生效）

- 翻译、解释或罗列 Stormworks 的部件 / 方块 / 组件名称
- 翻译游戏 UI、提示、报错、成就、任务与剧情文本
- 讨论 Lua 脚本、微控制器、逻辑节点、复合信号、属性标签
- 涉及载具名、地名、生物名、矿物名等专有名词
- 生成面向中文玩家的 Stormworks 文档、表格、教程、代码注释、字幕
- 校对用户提供的 Stormworks 中英文本

## 2. 标准动作（三步，缺一不可）

### 第 1 步：新鲜度检查（毫秒级）

```bash
"<PY>" "<WS>/汉化相关/脚本/sw_check.py" --auto
```

超过 7 天未更新、或创意工坊 `language.tsv` 内容发生变化时自动重建；否则直接退出。
退出码：`0` 最新 / `2` 需要更新但未执行 / `3` 失败。
**返回 3 时仍可继续查表，但须向用户说明对照表可能不是最新的。**

### 第 2 步：检索术语

```bash
"<PY>" "<WS>/汉化相关/脚本/sw_lookup.py" "关键词"
```

输出格式为 `中文 \t 英文 \t [模块/族] \t id`，可直接引用。

### 第 3 步：采用或回退

- 命中 → **原样采用**表中译法（含标点习惯，汉化补丁里的半角逗号句号不要擅自改全角）
- 未命中 → 才允许自行翻译，并沿用同族既有用词习惯（见第 5 节）

## 3. 命令速查（占位符先解析再复制）

```bash
PY="<PY>"                    # 由 工作区导航\脚本\sw_paths.py 解析；系统 python 3.8+ 亦可
G="<WS>/汉化相关/脚本"

"$PY" "$G/sw_check.py" --auto                        # 新鲜度检查 + 按需重建（最常用）
"$PY" "$G/sw_check.py" --json                        # 机器可读状态
"$PY" "$G/sw_lookup.py" "modular engine cylinder"    # 英文关键词
"$PY" "$G/sw_lookup.py" --zh 螺旋桨                  # 中文反查
"$PY" "$G/sw_lookup.py" --id def_giga_prop_small_name
"$PY" "$G/sw_lookup.py" --cat 引擎与动力 --limit 40   # 按模块浏览
"$PY" "$G/sw_lookup.py" --exact "Giant Propeller"     # 英文精确匹配
"$PY" "$G/update_glossary.py"                        # 强制完整同步（默认不联网）
```

## 4. 目录导览

| 用途 | 路径 |
| --- | --- |
| 入口说明与完整规则 | `汉化相关/README_中英对照表.md` |
| 精简速查（可整体读入上下文） | `汉化相关/常用术语速查.md` |
| 部件对照（16 个模块） | `汉化相关/词典/部件_*.md` |
| UI / 剧情 / 地名 / 生物名 | `汉化相关/词典/界面与文案.md` |
| EN→ZH 去重字典（5144 条） | `汉化相关/数据/sw_core_dict.json` |
| 全量条目（8795 条，每行一条 JSON） | `汉化相关/数据/sw_glossary.jsonl` |
| 全量 TSV（Excel / 人工校对） | `汉化相关/数据/sw_glossary.tsv` |
| 更新时间节点与源文件指纹 | `汉化相关/数据/last_update.json` |
| 变更历史 | `汉化相关/更新日志.md` |

`sw_glossary.jsonl` 字段：`id`、`kind`（`name`/`desc`/`s_desc`/`label`/`node_desc`/`ui`）、
`module`、`family`/`family_cn`、`topic`、`en`/`zh`、`state`（`ok`/`todo`）。

## 5. 翻译约定

| 场景 | 规范 |
| --- | --- |
| 表中已有 | 原样采用，不改标点、不改大小写 |
| 表中没有 | 沿用同族用词习惯后自译 |
| `###` | 数值占位符，保留原样 |
| `[$[action_xxx]]` | 按键提示占位符，保留原样 |
| 疑似补丁错译 | 以游戏内实际功能为准，并向用户说明 |

同族用词习惯参照：

- propeller→螺旋桨、rotor→旋翼/转子、thruster→推进器、pivot→枢轴、winch→绞盘
- modular engine→模块化引擎、cylinder→气缸、crankshaft→曲轴、manifold→歧管
- microcontroller→微控制器、composite→复合信号、node→节点、gate→逻辑门
- fluid / liquid / gas→流体 / 液体 / 气体、buoyancy→浮力、keel→龙骨、hull→船体

## 6. 自检清单（交付前对照）

1. 本次输出涉及的 Stormworks 专有名词，是否都查过对照表？
2. 是否沿用了表中译法，而非自己另起一套？
3. 占位符 `###`、`[$[action_xxx]]` 是否原样保留？
4. 若自行补译了新词，用词是否与同族既有译法一致？
5. 若 `sw_check.py` 返回 3，是否已向用户说明对照表可能过期？

## 7. 维护

> 🧭 本指南只讲**术语规则**。工作区的整体结构、脚本清单、环境依赖与长期知识见
> `<WS>\工作区导航\`（入口 `README.md`）；平台无关的 AI 契约见 `<WS>\AGENTS.md`。
> 已知的本工作区用词与汉化补丁字面不一致之处（复合信号 / 枢轴 / 物理传感器），
> 统一记录在 `工作区导航\05_项目长期知识.md` §7.1。


- 数据源以**创意工坊 `language.tsv` 为主**；GitHub 上游仅在其缺失时兜底。
  路径：`<SW_LANG_TSV>` = `<SW_WORKSHOP>\<工坊物品ID>\language.tsv`
  （**盘符与物品 ID 都不固定**，`2019972792` 只是当前补丁的 ID；`sw_paths.py` 按文件名搜索，
  真实值见 `工作区导航\路径配置.json`）
  上游：<https://github.com/BKN46/stormworks-translate-chn>
- 已注册 Windows 定时任务 **`StormworksGlossarySync`**（每天 10:00 跑 `sw_check.py --auto`），
  S4U 注册，不存密码、注销可运行、错过开机自动补跑，**不依赖任何 AI 工具**。
- 管理（PowerShell）：`Get-ScheduledTask` / `Start-ScheduledTask` / `Disable-ScheduledTask` /
  `Unregister-ScheduledTask`，任务名 `StormworksGlossarySync`。
- 注意：`schtasks.exe` 被本机安全策略拦截，请使用 PowerShell cmdlet。
- 发现明显误译或漏译，可到上游仓库提交 PR；本地待补翻清单见 `汉化相关/数据/待补翻清单.txt`。
