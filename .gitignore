# ============================================================
# 公开仓库（GitHub 协作版）排除清单
# 这些内容属「本机环境 / 第三方副本 / AI 应用私有状态」，
# 不随仓库分发；协作者按需自行准备（详见 CONTRIBUTING.md）。
# ============================================================

# 本地工具与第三方副本（任意层级；体积大或版权属第三方）
# steamcmd（Valve 二进制）、gh CLI 便携版、AI相关 爬虫工具等，勿入库
_tools/
pony IDE/

# AI 应用私有状态：记忆日志 / 自动化 / 目录联结（技能实体在 /技能库）
.workbuddy/
.workbuddy-ai/

# 本机路径实测配置（机器相关；sw_paths.py --set/--write 会按机器重建）
/工作区导航/路径配置.json

# 临时与暂存目录（任意层级）
_work/
# 工坊 Lua 提取暂存（本机提取库，体积大且跨轮累积，不入仓库；学习产出在 数据库/Lua/Lua示例）
AI相关/_提取暂存/

# 崩溃转储
*.dmp

# Python 缓存
__pycache__/
*.pyc

# 快捷方式（指向本机路径，跨机器无意义）
*.lnk

# OS / 编辑器杂项
Thumbs.db
Desktop.ini
.DS_Store
